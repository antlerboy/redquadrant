import Link from "next/link";
import { notFound } from "next/navigation";
import { buyerOffers } from "../../buyer-offers";
export function generateStaticParams(){return buyerOffers.map(o=>({slug:o.slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const o=buyerOffers.find(x=>x.slug===slug);return {title:o?.title,description:o?.summary,alternates:{canonical:`/offers/${slug}/`},openGraph:{title:o?.title,description:o?.summary,url:`/offers/${slug}/`}};}
export default async function Offer({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const o=buyerOffers.find(x=>x.slug===slug);if(!o)notFound();return <article className="site-frame offer-page"><p><Link href="/services">Services</Link></p><h1>{o.title}</h1><p className="offer-lead">{o.pressure}</p><p>{o.summary}</p><div className="offer-columns"><section><h2>What we do with you</h2><ul>{o.work.map(x=><li key={x}>{x}</li>)}</ul></section><section><h2>What you take forward</h2><ul>{o.outputs.map(x=><li key={x}>{x}</li>)}</ul></section></div><section><h2>Start with the situation</h2><p>Tell us what needs to change, the decisions already made, and the constraints you are working within. We will agree the scope, team, timing, and fee before work starts.</p><p><a className="rq-button" href={`mailto:benjamin.taylor@redquadrant.com?subject=${encodeURIComponent(o.title)}`}>Discuss your situation</a></p><p><Link href="/frameworks">Find a procurement route</Link></p></section><section><h2>Related work</h2><ul>{o.links.map(([url,label])=><li key={url}><a href={url}>{label}</a></li>)}</ul><p><Link href="/toolshed">Methods and practical learning in the Tool Shed</Link></p></section></article>}

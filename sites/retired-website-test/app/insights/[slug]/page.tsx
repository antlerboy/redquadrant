import {notFound} from "next/navigation";import Link from "next/link";import items from "../../insights-data.json";
export function generateStaticParams(){return items.map(({slug})=>({slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const item=items.find(i=>i.slug===slug);return item?{title:item.title,description:item.summary,alternates:{canonical:`/insights/${slug}/`}}:{};}
export default async function Insight({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const item=items.find(i=>i.slug===slug);if(!item)notFound();return <article className="site-frame offer-page"><p><Link href="/insights/">Insights</Link></p><h1>{item.title}</h1><p className="offer-lead">{item.summary}</p><p>RedQuadrant practice note</p>{item.paragraphs.map((p,i)=><p key={i}>{p}</p>)}<p><Link href={item.offer}>Explore support with this work</Link></p><p><a href="mailto:operations@redquadrant.com">Discuss your situation</a></p></article>}
